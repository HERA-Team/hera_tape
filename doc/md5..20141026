Feature Request:

    a method to check md5 hashes of files on tape against stored md5 hashes
in database


API:

  1. callable function to read file at current tape location
     take: file_name(_list?)
     return: md5sum

  2. modify paperdata.PaperDB.get_new to create a diftionary 
     of md5 hashes

  3. function: compare md5 results for one file on each tar
     file.

Test:

  [ description of how to test code with expected results ]
  [ optionally, include example test scripts of code ]



Owner: [username]:[date]
Review: [username]:[date]

